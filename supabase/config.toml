# Supabase local dev config skeleton. Run `supabase init` on your machine if
# this needs regenerating with a matching CLI version; edit project_id below.
project_id = "culinarycoreos"

[api]
enabled = true
port = 54321

[db]
port = 54322

[studio]
enabled = true
port = 54323
